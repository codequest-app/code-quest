# Main database (MySQL/PostgreSQL, optional)
# DATABASE_URL=mysql://user:password@localhost:3306/dbname

# SQLite path (always enabled, default: ./data/code-quest.db)
# DB_SQLITE_PATH=./data/code-quest.db

# File store - JSONL per session (optional)
# RAW_EVENT_DIR=./data/events
