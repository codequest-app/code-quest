/**
 * LocalFilesystemService tests moved to packages/summoner.
 * See: packages/summoner/src/__tests__/local-filesystem-service.test.ts
 */
import { describe, expect, it } from 'vitest';

describe('LocalFilesystemService', () => {
  it('tested in summoner package', () => {
    expect(true).toBe(true);
  });
});
