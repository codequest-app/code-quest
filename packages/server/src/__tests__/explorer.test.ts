/**
 * Explorer handler tests — now covered by local-filesystem-service.test.ts.
 * This file kept as a redirect notice; handler integration tested via file.test.ts pattern.
 */
import { describe, expect, it } from 'vitest';

describe('explorer handler', () => {
  it('is tested via LocalFilesystemService (see local-filesystem-service.test.ts)', () => {
    expect(true).toBe(true);
  });
});
