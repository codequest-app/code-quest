/**
 * Path validation tests — now covered by local-filesystem-service.test.ts
 * (validatePath is a private method of LocalFilesystemService).
 */
import { describe, expect, it } from 'vitest';

describe('explorer-utils (moved to LocalFilesystemService)', () => {
  it('path validation tested via browseDirectories in local-filesystem-service.test.ts', () => {
    expect(true).toBe(true);
  });
});
